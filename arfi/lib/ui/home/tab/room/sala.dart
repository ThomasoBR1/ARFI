import 'package:flutter/material.dart';

class LuzSala extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox.expand(
      child: Container(
        color: Colors.red
        ),
    );
  }
}
